info:
  name: Sign In (second user)
  type: http
  seq: 5

http:
  method: POST
  url: "{{BaseURL}}/auth/sign-in"
  body:
    type: json
    data: |-
      {
        "email": "{{demoEmail2}}",
        "password": "{{demoPassword}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
