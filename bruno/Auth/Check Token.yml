info:
  name: Check Token
  type: http
  seq: 4

http:
  method: GET
  url: "{{BaseURL}}/auth/check-token"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
