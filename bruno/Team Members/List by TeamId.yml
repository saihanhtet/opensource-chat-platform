info:
  name: List Members by TeamId
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseURL}}/team-members?teamId={{teamId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
