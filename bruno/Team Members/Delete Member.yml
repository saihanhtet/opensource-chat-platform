info:
  name: Delete Member
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{BaseURL}}/team-members/{{teamMemberId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
