info:
  name: Get Member by Id
  type: http
  seq: 4

http:
  method: GET
  url: "{{BaseURL}}/team-members/{{teamMemberId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
