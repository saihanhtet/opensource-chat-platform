info:
  name: Get Message by Id
  type: http
  seq: 4

http:
  method: GET
  url: "{{BaseURL}}/messages/{{messageId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
