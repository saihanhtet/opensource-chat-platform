info:
  name: Get Typing Status
  type: http
  seq: 7

http:
  method: GET
  url: "{{BaseURL}}/conversations/{{conversationId}}/typing"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Returns `{ "users": [...] }` for other participants currently marked as typing (in-memory TTL on the server).
