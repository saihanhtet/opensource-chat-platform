info:
  name: List My Conversations
  type: http
  seq: 2

http:
  method: GET
  url: "{{BaseURL}}/conversations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
