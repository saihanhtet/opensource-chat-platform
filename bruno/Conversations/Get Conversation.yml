info:
  name: Get Conversation by Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseURL}}/conversations/{{conversationId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
