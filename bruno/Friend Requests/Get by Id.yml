info:
  name: Get Friend Request by Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseURL}}/friend-requests/{{friendRequestId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
