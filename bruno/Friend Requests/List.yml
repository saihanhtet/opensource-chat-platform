info:
  name: List Friend Requests
  type: http
  seq: 2

http:
  method: GET
  url: "{{BaseURL}}/friend-requests"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
