info:
  name: Delete Friend Request
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BaseURL}}/friend-requests/{{friendRequestId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
