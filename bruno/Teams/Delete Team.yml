info:
  name: Delete Team
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BaseURL}}/teams/{{teamId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
