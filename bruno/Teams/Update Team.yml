info:
  name: Update Team
  type: http
  seq: 4

http:
  method: PUT
  url: "{{BaseURL}}/teams/{{teamId}}"
  body:
    type: json
    data: |-
      {
        "teamName": "Bruno Test Team (updated)",
        "description": "Updated via Bruno"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
