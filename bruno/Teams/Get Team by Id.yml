info:
  name: Get Team by Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseURL}}/teams/{{teamId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
