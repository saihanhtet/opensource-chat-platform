info:
  name: Update File (uploader only)
  type: http
  seq: 5

http:
  method: PUT
  url: "{{BaseURL}}/files/{{fileId}}"
  body:
    type: json
    data: |-
      {
        "fileName": "bruno-test-renamed.txt"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
