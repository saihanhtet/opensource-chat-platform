info:
  name: Delete File
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{BaseURL}}/files/{{fileId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
