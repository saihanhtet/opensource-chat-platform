info:
  name: List Files by ConversationId
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseURL}}/files?conversationId={{conversationId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
