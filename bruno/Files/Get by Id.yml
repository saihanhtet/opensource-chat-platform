info:
  name: Get File by Id
  type: http
  seq: 4

http:
  method: GET
  url: "{{BaseURL}}/files/{{fileId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
