info:
  name: Profile Update (JSON)
  type: http
  seq: 1

http:
  method: PUT
  url: "{{BaseURL}}/auth/profile"
  body:
    type: json
    data: |-
      {
        "username": "alice_live"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
